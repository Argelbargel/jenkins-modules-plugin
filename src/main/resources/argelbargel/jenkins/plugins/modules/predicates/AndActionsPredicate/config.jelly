<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry field="predicates">
        <f:hetero-list name="predicates" items="${instance.predicates}" hasHeader="true"
                       descriptors="${descriptor.all}"
                       targetType="${argelbargel.jenkins.plugins.modules.predicates.ActionsPredicate.class}"/>
    </f:entry>
</j:jelly>
