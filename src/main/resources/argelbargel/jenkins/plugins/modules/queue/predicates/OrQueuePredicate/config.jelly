<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
    <f:entry field="predicates">
        <f:hetero-list name="predicates" items="${instance.predicates}" hasHeader="true"
                       descriptors="${descriptor.allQueuePredicateDescriptors}"
                       targetType="${argelbargel.jenkins.plugins.modules.queue.predicates.QueuePredicate.class}"/>
    </f:entry>
</j:jelly>
